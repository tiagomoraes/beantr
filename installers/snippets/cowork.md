Use the Beantr skill for anything about my coffee: beans, gear, brew sessions, recipes, or recommendations.

Manage the coffee ledger at `${LEDGER_PATH}` (grant Cowork access to this folder if it isn't already connected). Current Markdown files are live state; monthly logs are append-only evidence; update recipes and insights only when evidence supports a stable recommendation. Never invent unknown coffee facts.
